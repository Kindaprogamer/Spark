const djs = require('djs-economy')
module.exports = {
    aliases: ['addcash'],
    category: 'Economy',
    ownerOnly: true,
    // /addcash userId amount
    callback: async ({message, args}) => {
    }
}